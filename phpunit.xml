<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!-- FR-018: UserDeletionLog::hashEmail() fails closed with no salt configured, so the
             suite needs one set. Not a real secret — test-only, checked into a committed file. -->
        <env name="GDPR_EMAIL_HASH_SALT" value="test-salt-do-not-use-in-production"/>
        <!-- MariaDB, not SQLite: the schema uses MariaDB-only DDL (AD-013).
             These are DDEV defaults, not a hard pin. PHPUnit sets an env entry only when the
             variable is absent (there is no force="true" here), so CI overrides them by exporting
             DB_*. Verified: DB_DATABASE=nonexistent_db makes the suite fail, so the environment
             wins over these values. -->
        <env name="DB_CONNECTION" value="mysql"/>
        <env name="DB_HOST" value="db"/>
        <env name="DB_PORT" value="3306"/>
        <env name="DB_DATABASE" value="test"/>
        <env name="DB_USERNAME" value="db"/>
        <env name="DB_PASSWORD" value="db"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
